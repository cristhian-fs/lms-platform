SERVER_PORT=5554
WEB_PORT=5555

# Absolute path on your machine where local course files (videos, images) live.
# Must match the paths stored in the database so the server can find them.
LOCAL_MEDIA_PATH=/run/media/cris/E8FA-42A1/courses

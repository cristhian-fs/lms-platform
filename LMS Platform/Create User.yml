info:
  name: Create User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/api/auth/sign-up/email
  body:
    type: json
    data: |-
      {
        "name": "cris",
        "email": "contato@cristhianf1.dev",
        "password": "Password1111!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # Necessário para o React Router / TanStack Router funcionar
    location / {
        try_files $uri $uri/ /index.html;
    }
}
